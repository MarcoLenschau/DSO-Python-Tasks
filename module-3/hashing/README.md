# HASH-CREATOR

## ✨ About the Application

This small Python tool reads a text file containing plaintext passwords, hashes each line (one password per line) and writes a new file containing the hashed passwords.

## ⭐ Features
    
    - Create hash list from password list with SHA-256